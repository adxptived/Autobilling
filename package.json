{
  "name": "card-generator-extension",
  "version": "1.3.0",
  "private": true,
  "description": "Browser extension for Luhn-valid card generation and Stripe autofill",
  "scripts": {
    "test": "node tests/luhn.test.js && node tests/core.test.js && node tests/manifest.test.js && node tests/build.test.js",
    "lint": "eslint -c config/eslint.config.js src scripts tests",
    "format": "prettier --config config/.prettierrc --write src scripts tests *.md package.json",
    "build": "node scripts/build.js",
    "zip": "npm run build && node scripts/zip.js"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "prettier": "^3.0.0"
  },
  "author": "adxptived",
  "license": "MIT"
}
